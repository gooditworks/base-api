datasource db {
  provider = "sqlite"
  url = "file:../database.sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model Pony {
  id Int @id @default(autoincrement())
  name String
  race String
}